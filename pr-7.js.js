//Convert character to ASCII values
var character=prompt("Enter the character for corresponding ASCII value:");
var ascii=character.charCodeAt(0);
console.log(`The corresponding ascii of ${character} is ${ascii}`);